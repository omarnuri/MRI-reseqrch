# Patient data never belongs in this repository — see docs/PRIVACY.md
*.zip
*.dcm
*.nii
*.nii.gz
dicom/
results/
spine_work/

# Environments and caches
.venv/
.venv312/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/

# Model weights (cached in Google Drive instead)
weights/
# `import totalspineseg` points nnUNet_results at a *relative* ./nnUNet_results, so
# a tool reading it from os.environ writes several hundred MB into the working
# directory. utils.child_env stops that; this is the second line of defence.
nnUNet_results/
nnUNet_raw/
nnUNet_preprocessed/
