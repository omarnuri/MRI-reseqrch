# spinelab — анализ МРТ позвоночника (исследовательский)

> **Не диагноз.** Инструмент считает измерения по маскам открытых сегментационных
> моделей и показывает, где они расходятся между сторонами и уровнями. Всё это
> требует проверки врачом-рентгенологом на исходных изображениях. Часть этапов —
> эвристики по яркости пикселей; в отчёте они помечены отдельно и находками не
> являются.

## Быстрый старт (Google Colab, слабый интернет учтён)

Открыть `notebooks/colab_pipeline.ipynb` в Colab, Runtime → GPU, выполнить 4 ячейки:

1. смонтировать Drive и указать каталог кэша весов;
2. поставить окружение (идемпотентно, ядро не перезапускается);
3. запустить пайплайн (продолжается с места обрыва);
4. посмотреть отчёт.

DICOM-архив кладётся **в Drive**, не в репозиторий (см. [docs/PRIVACY.md](docs/PRIVACY.md)).

## Локально / из командной строки

```bash
python -m spinelab run --dicom /path/study.zip --cache ~/spinelab-cache
python -m spinelab run --dicom /path/study.zip --quality --tta-mirror   # 24+ ГБ VRAM
python -m spinelab run --only marrow,posterior,report --force marrow
python -m spinelab run --dicom /path/study.zip --station 2   # второй блок исследования
python -m spinelab setup --sct --cache ~/spinelab-cache      # + Spinal Cord Toolbox (~3 ГБ)
python -m spinelab inspect --dicom /path/study.zip  # список серий, без GPU и dcm2niix
python -m spinelab audit --dicom /path/dicom       # какие PHI-теги есть в заголовках
python -m spinelab deid  --dicom /path/dicom --out /path/anon
python -m spinelab stages                          # список этапов
```

Если исследование покрывает два уровня позвоночника (например, шейный и поясничный),
`inspect` покажет это как две станции, а прогон анализирует одну за раз: `--station 1`,
затем `--station 2`. Результаты пишутся в `results/station-N`.

Тесты (без GPU и без данных, ~15 с):

```bash
python -m pytest -q
```

## Что делает пайплайн

| Этап | Инструмент | Что даёт | Уровень |
|---|---|---|---|
| `ingest` | dcm2niix | NIfTI, инвентарь серий, выбор последовательностей, PHI-аудит | измерение |
| `spineps` | SPINEPS 2.0 (+ VERIDAH labeling) | тела позвонков, **задние элементы по сторонам**, нумерация | модель |
| `totalspineseg` | TotalSpineSeg | спинной мозг, канал, диски по уровням | модель |
| `totalsegmentator` | TotalSegmentator MRI | параспинальные мышцы, рёбра | модель |
| `seg_sct` | Spinal Cord Toolbox 7.3 | спинной мозг и канал теми моделями, под которые подогнаны пороги | модель |
| `register` | SimpleITK | совмещение масок с fat-sat и аксиальной серией (поправка на движение) | измерение |
| `fatsat_qc` | — | работает ли подавление жира; гейт для всей ветки про отёк | эвристика |
| `crosscheck` | TotalSegmentator `vertebrae_mr` | согласие двух независимых моделей по уровням (профиль quality) | модель |
| `facets_axial` | — | фасеточные суставы по уровням и сторонам на аксиальной T2 | эвристика |
| `geometry` | — | высоты тел, углы клиновидности, правило Шейермана, кривизна | измерение |
| `muscles` | — | объём и лево-правая асимметрия мышц | измерение |
| `marrow` | — | неоднородность сигнала в телах позвонков (**только при fat-sat**) | эвристика |
| `posterior` | — | сравнение левых и правых фасеточных / поперечных отростков | эвристика |
| `canal` | — | профиль площади канала, относительное сужение | измерение |
| `compression` | SCT | шейный канал: вероятность компрессии и aSCOR **против опубликованных порогов** | **порог из внешней когорты** |
| `discs` | — | относительный сигнал дисков по уровням | измерение |
| `radiomics` | — | first-order + GLCM по телам позвонков | измерение |
| `agreement` | — | Dice спинного мозга между двумя сегментаторами (контроль) | измерение |
| `report` | — | один HTML-отчёт + `findings.json` | — |

Каждый этап пишет `results/stages/<этап>.json`. Повторный запуск читает их и
продолжает с места обрыва — Colab на слабом канале отваливается регулярно.

## Устройство

```
spinelab/
  config.py      параметры прогона, кэш весов в Drive, пути
  labels.py      метки апстрима (TPTBox Location, tss_map.json) в одном месте
  sequences.py   распознавание и выбор последовательностей (fat-sat, плоскость)
  analysis.py    чистая математика — всё покрыто тестами
  pipeline.py    раннер этапов с продолжением и изоляцией ошибок
  evidence.py    уровни доказательности
  dicom_audit.py аудит и деидентификация DICOM
  stages/        по одному модулю на этап
tests/           333 теста, включая сквозной прогон на синтетическом фантоме
notebooks/
  colab_pipeline.ipynb   рабочий лаунчер (4 ячейки)
  legacy/                старый ноутбук на 57 ячеек, часть выводов отозвана
docs/
  COLAB.md                  пошаговая инструкция запуска (A100)
  bug-ledger-2026-07-27.md  26 дефектов старого ноутбука: причина, фикс, проверка
  self-audit-2026-07-27.md  аудит нового кода: 3 реальных бага, что осталось непроверенным
  stack-review-2026-07.md   актуальность стека на июль 2026, что выброшено и почему
  stack-review-2026-08.md   что добавил SCT: первые пороги из внешних когорт
  research/                 внешние обзоры, журнал поиска данных, список литературы
                            и каталог критериев (reading-list-2026-08.md)
  clinical-context.md       что эти данные могут и не могут показать
  study-2026-08-21.md       разбор августовского исследования: две станции, что снято
  audit-2026-08-22.md       аудит проекта: что проверено, что устарело, что срочно
  prompts/                  промпты для внешних исследований стека:
                            model-landscape (позвоночник, открытые веса) и
                            imaging-model-landscape (вся анатомия + коммерческие)
  PRIVACY.md                PHI в публичном репозитории: что делать
```

## Что известно про эти исследования

**Май 2026, грудной отдел** (FUJIFILM 1.5 Тл): сагиттальные T2 и T1 (17 срезов,
3.5 мм), коронарный STIR (23 среза, 4 мм, TI 100 мс — единственная серия с
подавлением жира), аксиальная T2 (66 срезов, 4 мм, 0.49 мм в плоскости) и скаут.

**Август 2026, шейный + поясничный отделы** (Siemens MAGNETOM Sola 1.5 Тл): 26 серий,
из них 8 диагностических, разделённых на две станции — шейную и поясничную.
На каждой: сагиттальные T2 и T1 (3 мм, 0.27–0.39 мм в плоскости), аксиальная T2
(35 и 27 срезов) и 3D SPACE с подавлением жира. Средний и нижний грудной отдел
между станциями не снят; STIR нет. Подробно — [docs/study-2026-08-21.md](docs/study-2026-08-21.md).

Аксиальная серия — самая информативная плоскость для фасеточных суставов; старый
ноутбук её не использовал ни для чего. Отёк оценивается только по серии с подавлением
жира и коротким TE, и только если проверка `fatsat_qc` подтвердит, что жир
действительно подавлен.

## Главное ограничение

Без последовательности с подавлением жира (STIR / TIRM / SPAIR / Dixon) отёк
костного мозга и периартикулярный отёк **не оцениваются** — и пайплайн в этом
случае отказывается выдавать числа, а не выдаёт их с оговоркой. Без аксиальной
(или косой) плоскости грудные фасеточные суставы почти не видны. Подробно —
[docs/clinical-context.md](docs/clinical-context.md).
